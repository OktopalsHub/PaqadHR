interface:
  display_name: "PaqadHR"
  short_description: "Repo-specific patterns and workflows for PaqadHR"
  default_prompt: "Use the PaqadHR repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true