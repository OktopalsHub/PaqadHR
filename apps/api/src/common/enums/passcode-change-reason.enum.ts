export enum PasscodeChangeReason {
    INITIAL_SETUP = 'initial_setup',
    USER_REQUESTED = 'user_requested',
    SECURITY_RESET = 'security_reset',
    ADMIN_RESET = 'admin_reset',
    EXPIRED = 'expired'
}
